import UIKit
public struct UniversalBrowser {
    public private(set) var text = "Hello, World!"

    public init() {
//        if let vc = UIStoryboard(name: "UBBoard", bundle: nil).instantiateViewController(withIdentifier: "HomeVC") as? HomeVC {
//            self.present(vc)
//        }
    }
}
